# 数据组织快速指南

## 🎯 核心思路

**分层存储，按需访问** - 根据数据使用频率和重要性，分为4个层级：

1. **汇总数据** (必须) - 快速查看实验结果
2. **统计数据** (必须) - 进行数据分析和可视化  
3. **详细记录** (可选) - 深入分析特定案例
4. **原始日志** (自动) - 调试和完整追溯

## 📁 目录结构（简化版）

**重要**: 所有数据都保存在带时间戳的实验目录下，例如：
- `results_rainbowdqn/rl_generation_rainbowdqn_test_20251105_085316/arena_hard_20251105_085316/`

每次运行都会创建新的时间戳文件夹，避免混乱。Rainbow DQN结果保存在独立的 `results_rainbowdqn/` 根目录下，与baseline结果分离。

```
{timestamp_dir}/  # 例如: results_rainbowdqn/rl_generation_rainbowdqn_test_20251105_085316/arena_hard_20251105_085316/
├── config/           # 配置和元数据
├── summary/          # 汇总数据（快速查看）
├── training/         # 训练过程数据
├── evaluation/       # 评估数据
├── judge_logs/       # Judge输入输出
└── logs/             # 文本日志
```

## 💾 关键文件说明

### 必须保存的文件（小，快速访问）
- `summary/training_summary.json` - 训练结果汇总
- `summary/test_summary.json` - 测试结果汇总
- `summary/attack_usage_stats.json` - 攻击方法使用统计 ⭐
- `summary/episode_statistics.csv` - Episode统计（便于分析）

### 可选保存的文件（大，按需）
- `evaluation/test_samples/*/sample_*.json` - 每个样本的详细记录
- `judge_logs/*/detailed/judge_call_*.json` - 每次judge调用的详细记录
- `training/episodes/episode_*.json` - 每个episode的详细数据

### 自动保存的文件
- `logs/*.log` - 所有文本日志

## 🔍 数据查找策略

### 快速查看结果
```bash
# 查看整体结果
cat summary/training_summary.json
cat summary/test_summary.json

# 查看攻击方法使用情况（你提到的关键需求）
cat summary/attack_usage_stats.json
```

### 查找特定样本
```bash
# 1. 先查索引
cat evaluation/test_samples/index.json

# 2. 再加载详细数据
cat evaluation/test_samples/successful/sample_001.json
```

### 查找Judge调用记录
```bash
# 1. 先看汇总
cat judge_logs/test/judge_logs_summary.json

# 2. 再查详细（如果需要）
cat judge_logs/test/detailed/judge_call_*.json
```

## ⚙️ 实现时的配置选项

建议在训练脚本中添加：

```python
--save_detail_level {summary|statistics|detailed|all}
  # summary: 只保存汇总数据（默认）
  # statistics: 保存汇总+统计数据
  # detailed: 保存汇总+统计+详细记录
  # all: 保存所有数据

--save_judge_logs {summary|detailed|none}
  # summary: 只保存统计信息（默认）
  # detailed: 保存每次调用的完整输入输出
  # none: 不保存（不推荐）

--save_episode_details {none|checkpoints|all}
  # none: 不保存详细episode数据（默认）
  # checkpoints: 只在评估点保存
  # all: 保存所有episode
```

## 📊 数据组织的好处

1. **快速访问**: 汇总数据小而快，一眼看到结果
2. **按需加载**: 详细数据大而全，需要时再加载
3. **节省空间**: 重要数据优先，详细数据可选
4. **易于分析**: CSV格式便于pandas/Excel处理
5. **便于查找**: 索引文件快速定位

## 🎨 实际使用场景

### 场景1: 快速查看实验结果
→ 直接看 `summary/` 目录下的JSON文件

### 场景2: 分析攻击方法使用情况
→ 看 `summary/attack_usage_stats.json` + `summary/episode_statistics.csv`

### 场景3: Case Study某个成功样本
→ 从 `evaluation/test_samples/index.json` 找到样本ID
→ 加载 `evaluation/test_samples/successful/sample_XXX.json`

### 场景4: 分析Judge行为
→ 看 `judge_logs/*/judge_logs_summary.json` 了解整体情况
→ 需要详细时再看 `judge_logs/*/detailed/` 下的文件

### 场景5: 调试问题
→ 查看 `logs/` 目录下的日志文件

## 💡 建议

1. **默认配置**: 使用 `summary` + `statistics` 级别，平衡信息量和文件大小
2. **Case Study时**: 临时开启 `detailed` 级别，保存详细数据
3. **定期清理**: 压缩或归档旧的详细日志文件
4. **使用索引**: 通过索引文件快速定位，避免遍历所有文件

